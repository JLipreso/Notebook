# Placeholder — keeps apps/ in the tree until the first Vue app lands here.
# One folder per deployable front end, each its own pnpm workspace package.
# See CLAUDE.md §2. Delete this file once a real app exists.
