{
  "name": "@notebook/services",
  "version": "1.0.0",
  "private": true,
  "description": "Notebook API layer — axios singleton + the ONE mock/live datasource switch (CLAUDE.md §3 golden rule)",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@notebook/types": "workspace:*",
    "axios": "^1.7.9",
    "firebase": "^12.19.0"
  },
  "devDependencies": {
    "typescript": "~5.7.0"
  }
}
